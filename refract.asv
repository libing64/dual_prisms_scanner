function r2 = refract(r1, n1, n)

      d = cross(r1, n1);
      d = d / norm(d);
      
      beta1 = acos(dot(r1, n1) / (norm(r1) * norm(n1)) );
      beta2 = asin(sin(beta1) / n);
      
      
      R = angle2dcm(d(1) * beta, d);

end
